/**
 * Enum para definir os tipos de utilizador aceites na ServeRest
 * @description Garante que apenas valores válidos sejam passados para o campo 'administrador'
 */
export enum UserType {
  ADMIN = "true",
  COMMON = "false"
}